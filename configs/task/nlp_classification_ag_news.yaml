# @package _global_
defaults:
  - override /optimizer: adamw_text_scratch
  - override /model: vanilla_1d_small
  - override /datamodule: ag_news

task:
  name: "nlp_classification"

trainer:
  max_epochs: 10
  accumulate_grad_batches: 2
  precision: 32
